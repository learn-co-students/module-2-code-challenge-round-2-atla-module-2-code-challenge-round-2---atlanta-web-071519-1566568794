<%= form_for(@appearance) do |f| %>
    Guest: <%= f.collection_select :guest_id, Guest.all, :id, :name %>
    Episode: <%= f.collection_select :episode_id, Episode.all, :id, :number %>
    Rating: <%= f.number_field :rating %>
    <%= f.submit %>
    <% end %>
